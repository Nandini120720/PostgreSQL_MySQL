	
1. create new schema on local as copy of another one schema.

•	Export the source schema :
mysqldump -u root -p --single-transaction --routines --triggers --databases sample_db > sample_db.sql

•	Create the new schema manually (if not already in dump):
   CREATE DATABASE new_schema_name;

•	Import the dump file into the new schema:
   mysql -u root -p new_schema_name < source_
db.sql
   

2. Please provide user list of prdwhsdb01 in both excel and word formats.

•	Get the MySQL User List :
SELECT user, host FROM mysql.user;

•	Export the User List :
mysql -u root -p -e "SELECT user, host FROM mysql.user;" > appuserr_prdwhsdb01.csv

sed 's/\t/,/g' appuserr_prdwhsdb01.csv > appuserr_prdwhsdb01_excel.csv


OR


SELECT *
FROM mysql.user
INTO OUTFILE '/var/lib/mysql-files/mysql_user.csv'
FIELDS TERMINATED BY ','
OPTIONALLY ENCLOSED BY '"'
ESCAPED BY '"'
LINES TERMINATED BY '\n';


3. Provide access for three different databases to dwjob user:
Give dwjob “read/write” access (i.e., SELECT, INSERT, UPDATE, DELETE).


•	create 3 database :
create database sales_db;
create database finance_db;
create database inventory_db;

•	Grant Privileges 
GRANT SELECT, INSERT, UPDATE, DELETE ON sales_db.* TO 'appuserr'@'%';
GRANT SELECT, INSERT, UPDATE, DELETE ON finance_db.* TO 'appuserr'@'%';
GRANT SELECT, INSERT, UPDATE, DELETE ON inventory_db.* TO 'appuserr'@'%';
FLUSH PRIVILEGES; ----Apply Changes

SHOW GRANTS FOR 'appuserr'@'%'; ---Verify the Grants



4. Copy 2 databases from one to another server.
Source server: source_host (e.g., prdwhsdb01)
Target server: target_host (e.g., replica01)
Databases to copy: db1, db2

•	Dump the Databases
mysqldump -h source_host -u root -p --single-transaction --routines --triggers --databases db1 db2 > db_copy.sql

•	Import into Target Server
mysql -h target_host -u root -p < db_copy.sql





5. Copy 1 database from one to another server with different target db name:

Source server: source_host
Source database: db_source
Target server: target_host
Target database (new name): db_target


•	Dump the Source Database :
mysqldump -h source_host -u root -p --single-transaction --routines --triggers --databases db_source > db_source.sql

•	Replace the Source DB Name with Target DB Name :
This changes every mention of db_source to db_target (usually in CREATE DATABASE and USE statements):
sed 's/`db_source`/`db_target`/g' db_source.sql > db_target.sql

•	Create the Target Database :
Before importing, ensure the target database exists :
mysql -h target_host -u root -p -e "CREATE DATABASE IF NOT EXISTS db_target;"

•	Import the Modified Dump File :
mysql -h target_host -u root -p db_target < db_target.sql (Now db_source is fully copied to db_target on the new server.)

•	After import, verify on the target server :
SHOW TABLES FROM db_target;


lat uat